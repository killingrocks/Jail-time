import java.awt.Graphics;
public class Bank extends Sprite {
	
			Bank(){
				super("bank.png"); //the bank class is set to 300
				
				}
			public void updateImage(Graphics g) {
				super.updateImage(g);
				//setX(300);
				//setY(300);
				
			}
		}
